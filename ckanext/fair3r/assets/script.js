/**
 * Module for Fair3R UI behavior hooks.
 * Options:
 * - debug: enable console logging
 */
ckan.module("fair3r-module", function ($, _) {
  "use strict";
  return {
    options: {
      debug: false,
    },

    initialize: function () {},
  };
});
